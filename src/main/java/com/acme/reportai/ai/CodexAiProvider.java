package com.acme.reportai.ai;

public class CodexAiProvider extends OpenAiCompatibleProvider {
    public CodexAiProvider(String baseUrl, String model, String apiKey) {
        super(baseUrl, model, apiKey);
    }
}
